[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install -g corepack@0.24.1 && corepack enable", "yarn install --immutable"]

[phases.build]
cmds = ["yarn build"]

[start]
cmd = "yarn start"
